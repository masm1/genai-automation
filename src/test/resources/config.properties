# Environment
env=qa

# Application URLs
baseUrl=https://www.saucedemo.com/
apiBaseUrl=https://reqres.in/api

# Browser Configuration
browser=chrome
headless=false
grid=false
gridUrl=http://localhost:4444

# Timeouts
implicitWait=10
explicitWait=20
pageLoadTimeout=30

# Credentials (for demo only, real project uses secret vault)
username=standard_user
password=secret_sauce